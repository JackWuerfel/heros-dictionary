import { CardProps } from './types';

declare const Card: ({ alignmentVertical, alignmentHorizontal, classes, data, direction, size, marginTop, marginBottom, }: CardProps) => import("react/jsx-runtime").JSX.Element;
export default Card;
